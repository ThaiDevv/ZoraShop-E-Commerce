spring:
  application:
    name:
      ZoraShop-MiniShopeeE-CommercePlatform
  datasource:
    url: jdbc:postgresql://localhost:5432/zorashop_db
    username: zorashop_user
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: on
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 50
        order_updates: true

  flyway:
    enabled: true
    baseline-on-migrate: true
jwt:
  secret: "Tranvanthai26@DayLaSecretCuaJwtDungLayCuaToi"
  access-expiration: ${JWT_ACCESS_EXPIRATION:86400000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}