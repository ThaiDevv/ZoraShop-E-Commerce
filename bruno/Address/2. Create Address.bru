meta {
  name: 2. Create Address
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/users/me/addresses
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "fullName": "Nguyen Van A",
    "phone": "0987654321",
    "street": "123 Đường Le Loi",
    "ward": "Phường 1",
    "district": "Quận 1",
    "city": "TP. Hồ Chí Minh",
    "isDefault": true
  }
}

script:post-response {
  if (res.body && res.body.data && res.body.data.id) {
    bru.setVar("addressId", res.body.data.id);
  }
}
